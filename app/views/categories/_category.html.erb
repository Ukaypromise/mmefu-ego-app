<div id="<%= dom_id category %>" class="render-category">
  <p>
    <%= image_tag category.icon, alt: 'icon' %>
  </p>

  <p>
    <% if can? :read, category %>
      <%= link_to category.name, category_transaction_categories_path(category.id) %>
    <% end %>
  </p>

  <p>
    
  </p>
</div>